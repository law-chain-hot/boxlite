{
  "name": "api-client-go",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "api-client-go",
  "tags": [],
  "targets": {
    "test": {
      "executor": "@nx-go/nx-go:test"
    },
    "build": {
      "executor": "@nx-go/nx-go:build",
      "inputs": ["goProduction"],
      "options": {
        "main": "./{projectRoot}",
        "outputPath": "dist/libs/api-client-go"
      }
    },
    "lint": {
      "executor": "@nx-go/nx-go:lint"
    },
    "generate:api-client": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": ["goApiClient"],
      "outputs": [
        "{projectRoot}/*.go",
        "{projectRoot}/go.mod",
        "{projectRoot}/go.sum",
        "{projectRoot}/api/openapi.yaml"
      ],
      "options": {
        "commands": [
          "rm -f {projectRoot}/*.go",
          "yarn run openapi-generator-cli generate --skip-validate-spec --git-repo-id=boxlite/libs/api-client-go --git-user-id=boxlite-ai -i dist/apps/api/openapi.json -g go --additional-properties=disallowAdditionalPropertiesIfNotPresent=false,packageName=apiclient,moduleVersion=$DEFAULT_PACKAGE_VERSION,generateInterfaces=true,enumClassPrefix=true,structPrefix=true -o {projectRoot}",
          "bash hack/go-client/postprocess.sh {projectRoot} apiclient api-client-go"
        ],
        "parallel": false
      },
      "dependsOn": [
        {
          "target": "openapi",
          "projects": "api"
        }
      ]
    },
    "tidy": {
      "executor": "@nx-go/nx-go:tidy"
    },
    "check-version-env": {},
    "set-version": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{workspaceRoot}/apps/cli/go.mod",
        "{workspaceRoot}/libs/sdk-go/go.mod",
        { "env": "VERSION" },
        { "dependentTasksOutputFiles": "**/*", "transitive": true }
      ],
      "outputs": ["{workspaceRoot}/apps/cli/go.mod", "{workspaceRoot}/libs/sdk-go/go.mod", "{projectRoot}/VERSION"],
      "options": {
        "cwd": "{workspaceRoot}",
        "commands": [
          "cd cli && go mod edit -require=github.com/boxlite-ai/boxlite/libs/api-client-go@$VERSION",
          "cd ../sdks/go && go mod edit -require=github.com/boxlite-ai/boxlite/libs/api-client-go@$VERSION",
          "cd otel-collector/exporter && go mod edit -require=github.com/boxlite-ai/boxlite/libs/api-client-go@$VERSION",
          "echo \"$VERSION\" > api-client-go/VERSION"
        ],
        "parallel": false
      },
      "dependsOn": ["check-version-env"]
    }
  }
}
