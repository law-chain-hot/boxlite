{
  "name": "otel-collector",
  "$schema": "../../runner_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/otel-collector",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "mkdir -p dist/apps && go run -mod=mod go.opentelemetry.io/collector/cmd/builder@v0.144.0 --config {projectRoot}/builder-config.dev.yaml"
        ],
        "env": {
          "GOWORK": "off"
        }
      },
      "configurations": {
        "production": {
          "commands": [
            "mkdir -p dist/apps && if [ -d apps/otel-collector/exporter ]; then CONFIG=apps/otel-collector/builder-config.yaml; else CONFIG={projectRoot}/builder-config.local-production.yaml; fi; go run -mod=mod go.opentelemetry.io/collector/cmd/builder@v0.144.0 --config \"$CONFIG\""
          ]
        }
      },
      "cache": true,
      "outputs": ["{workspaceRoot}/dist/apps/otel-collector/**/*"],
      "inputs": ["default", "!{projectRoot}/config.yaml", "!{projectRoot}/config.dev.yaml"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["dist/apps/otel-collector/boxlite-otel-collector --config {projectRoot}/config.dev.yaml"]
      },
      "configurations": {
        "production": {
          "commands": ["dist/apps/otel-collector/boxlite-otel-collector --config {projectRoot}/config.yaml"]
        }
      },
      "dependsOn": ["build"]
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd {projectRoot}/exporter && go fmt ./..."
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd {projectRoot}/exporter && go test ./..."
      }
    },
    "lint": {
      "executor": "@nx-go/nx-go:lint"
    },
    "check-version-env": {},
    "docker": {
      "options": {
        "target": "otel-collector"
      }
    },
    "push-manifest": {}
  }
}
